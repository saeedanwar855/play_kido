import 'package:flutter/material.dart';

class AphabitsCard extends StatelessWidget {
  const AphabitsCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Text('Alphabits'),
      ],
    );
  }
}
